{
  "devDependencies": {
    "tailwindcss": "^4.1.16"
  },
  "dependencies": {
    "mysql2": "^3.22.5"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  }
}
